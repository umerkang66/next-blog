import classes from '../../styles/components/ layout/logo.module.css';
import { FC, ReactElement } from 'react';

const Logo: FC = (props): ReactElement => {
  return <div>Umer | Umer's Blog</div>;
};

export default Logo;
